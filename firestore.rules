
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isSignedIn() && request.auth.uid == userId;
    }

    // Function to check for Admin role from custom claims
    function isAdmin() {
      return request.auth.token.role == 'Admin';
    }

    // Function to check for Admin or Editor role from custom claims
    function isAdminOrEditor() {
      return request.auth.token.role == 'Admin' || request.auth.token.role == 'Editor';
    }
    
    // User profiles can be read by their owner or an admin.
    // List is only for admins.
    // Updates can only be done by the owner. Role changes are handled by a Cloud Function.
    match /users/{userId} {
      allow get: if isOwner(userId) || isAdmin();
      allow list: if isAdmin();
      allow create: if isOwner(userId);
      allow update: if isOwner(userId);
      allow delete: if isAdmin();
    }

    // Categories can be read by anyone.
    // Writes are restricted to Admins and Editors.
    match /categories/{categoryId} {
      allow get, list: if true;
      allow create, update, delete: if isAdminOrEditor();
    }

    // Folders can be read by anyone.
    // Create is allowed for any signed-in user.
    // Update/Delete is restricted to the folder's creator or an admin.
    match /folders/{folderId} {
      allow get, list: if true;
      allow create: if isSignedIn();
      allow update: if isOwner(resource.data.createdBy);
      allow delete: if isOwner(resource.data.createdBy) || isAdmin();
    }

    // Tags can be read by anyone.
    // Writes are restricted to Admins and Editors.
    match /tags/{tagId} {
      allow get, list: if true;
      allow create, update, delete: if isAdminOrEditor();
    }
    
    // Documents can be read by anyone.
    // Create is allowed for any signed-in user.
    // Update/Delete is restricted to the document's creator or an admin.
    match /documents/{documentId} {
      allow get, list: if true;
      allow create: if isSignedIn();
      allow update: if isOwner(resource.data.createdBy);
      allow delete: if isOwner(resource.data.createdBy) || isAdmin();
    }
    
    // Audit logs can only be read by their owner.
    // Creation should be handled by server-side logic (e.g., Cloud Functions).
    match /users/{userId}/auditLogs/{logId} {
      allow get, list: if isOwner(userId);
      allow create, update, delete: if false;
    }
  }
}
